# Filtres — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remplacer la jointure naïve des filtres par le type `Filter` et `build_query`, pour que la requête envoyée à GitHub porte toujours `is:pr` en tête et `sort:updated-desc` en queue, et qu'un nouveau filtre ne coûte qu'une modification de `filter.rs`.

**Architecture:** `filter.rs` devient le seul endroit qui connaît la syntaxe de recherche de GitHub côté requête. Il expose une énumération `Filter`, la traduction d'une variante en fragment (`fragment`), la reconnaissance d'une chaîne de réglages en variante (`Filter::parse`), et l'assemblage de la chaîne complète (`build_query`). `app` traduit une fois les chaînes des réglages en variantes, puis assemble la requête à chaque demande et la transmet dans `Command::Fetch`. `github` reçoit une chaîne toute faite et n'assemble plus rien : `search_query` disparaît. `filter` reste pur — ni réseau, ni terminal — donc entièrement testable en tests unitaires.

**Tech Stack:** Rust édition 2021, bibliothèque standard uniquement pour `filter.rs`. Aucune nouvelle dépendance.

**Spec:** `docs/specs/02-filtres.md` (contexte transverse : `docs/specs/00-fondations.md` pour les règles de dépendance et le fichier de réglages, `docs/specs/01-modele-et-donnees.md` pour la requête `LIST` qui consomme la chaîne)

Aucun lien de design n'a été fourni pour cette spec, et elle n'ajoute aucun écran ni aucun élément d'interface : elle ne change que la chaîne envoyée à GitHub. La vérification à l'œil de la tâche 4 se fait donc dans le terminal, pas avec `claude-in-chrome`, qui ne sait pas piloter une interface en mode texte.

## Conditions d'exécution

- Exécution en **subagent-driven development** : un sous-agent par tâche, revue entre chaque.
- Branche de travail : `feat/filtres`, créée depuis `develop` à la première étape de la tâche 1. Jamais de travail direct sur `develop`, jamais de pull request vers `main`.
- Ce fichier de plan n'est pas encore suivi par git : l'ajouter au premier commit de la branche.
- Le registre `sdd` du dépôt est vide au départ (`.superpowers/sdd/` ne contient que son `.gitignore`) : repartir de zéro, aucun état antérieur à reprendre.
- Aucune question à poser avant de commencer : tout ce qui est nécessaire est dans ce plan et dans la spec. Une question n'est légitime que devant un blocage réel qui empêche la suite — un outil absent, un service indisponible.
- Une décision **mise de côté** se consigne dans `docs/suivi/DETTE.md`, au format déjà en place dans ce fichier, et uniquement si elle est critique pour la suite. Pas les décisions prises, pas les idées d'amélioration, aucune sur-conception.
- Si un blocage réel empêche la suite — outil absent, service indisponible, jeton introuvable là où une étape en a besoin — poser la question plutôt que de contourner.
- À la fin : pull request vers `develop`, puis un rapport qui ne raconte pas le travail fait. Voir la tâche 4, étapes 8 et 9.

## Global Constraints

- Rust, édition 2021. Binaire unique nommé `owl`, aucune sous-commande.
- **Aucune nouvelle dépendance**, ni de production ni de développement.
- **Aucun nouveau fichier source.** L'arborescence reste exactement celle de `00-fondations.md`.
- Dépendances à sens unique : `filter` ne dépend que de la bibliothèque standard ; `app` dépend de `model` et de `filter` et ne fait aucun appel réseau ; une fonction de dessin ne modifie jamais l'état et ne décide de rien.
- Correspondances filtre → fragment, au caractère près (`02-filtres.md`) :
  | Filtre | Fragment |
  |---|---|
  | `AuthoredByMe` | `author:@me` |
  | `ReviewRequestedFromMe` | `review-requested:@me` |
  | `AssignedToMe` | `assignee:@me` |
  | `Open` | `is:open` |
  | `Draft(true)` | `draft:true` |
  | `Draft(false)` | `draft:false` |
  | `Org(o)` | `org:o` |
  | `Repo(r)` | `repo:r` |
  | `Label(l)` | `label:"l"` |
  | `Raw(s)` | `s` tel quel |
- `build_query` ajoute **toujours** `is:pr` en tête et `sort:updated-desc` en queue, quels que soient les filtres. Les fragments des filtres se placent entre les deux, dans l'ordre reçu, séparés par une seule espace.
- Exemple de référence : les filtres par défaut donnent exactement `is:pr author:@me is:open sort:updated-desc`.
- Une chaîne de réglages non reconnue devient `Filter::Raw` et se retrouve intacte dans la requête.
- Une liste `filters` vide est refusée au démarrage avec le message déjà en place : « Aucun filtre actif : la recherche ramènerait tout GitHub. » La garde reste dans `config.rs`.
- Ajouter un filtre doit rester trois modifications dans le seul `filter.rs` : une variante, une ligne dans `fragment`, une ligne dans `parse`.
- Le jeton n'est jamais écrit dans un fichier, ni journalisé, ni affiché.
- Le projet est en français : messages affichés, commentaires, messages de commit. Les identifiants du code restent en anglais.
- `cargo build`, `cargo test`, `cargo clippy -- -D warnings` et `cargo fmt --check` doivent passer à la fin de chaque tâche. Aucune n'est optionnelle.

## Décisions mises de côté

Toute décision **reportée** pendant l'exécution se consigne dans `docs/suivi/DETTE.md`, au format déjà en place dans ce fichier. Uniquement ce qui est reporté et critique pour la suite.

L'entrée existante « La chaîne de recherche est une jointure de filtres » est **résolue** par ce plan : elle sort du fichier à la tâche 3. Le registre n'est pas un historique.

## Décisions prises en écrivant ce plan

Elles sortent du texte de la spec et sont donc reportées dans `docs/specs/02-filtres.md` par la tâche 3, comme l'exige l'ordre de vérité du `CLAUDE.md`.

1. **`Command::Fetch` porte la chaîne, pas les filtres.** `app` assemble la requête avec `filter::build_query` et transmet `query: String`. `github::fetch_pull_requests` reçoit `&str` et ne construit plus rien ; `search_query` est supprimée. L'assemblage vit ainsi à un seul endroit, testable sans réseau, et le test de `app` vérifie la chaîne exacte de bout en bout.
2. **`Config::filters` reste `Vec<String>`.** C'est ce que dit la note d'implémentation de la spec : les réglages portent des chaînes, et c'est `app` qui les traduit en variantes. `config.rs` n'est donc pas modifié, et `filter` ne dépend pas de `config`.
3. **`app` traduit une seule fois, à la construction.** `App::new` range un `Vec<Filter>` et `fetch` appelle `build_query` à chaque demande. Un futur changement de filtre depuis l'écran n'aura qu'à modifier ce vecteur.
4. **Pas de déduplication.** Si les réglages contiennent déjà `is:pr`, il apparaît deux fois dans la requête. La spec dit « toujours en tête, quels que soient les filtres » ; GitHub accepte le doublon, et reconnaître ce cas ajouterait une règle que personne n'a demandée.
5. **Un fragment vide est écarté de l'assemblage.** Une chaîne blanche dans les réglages produirait sinon une double espace dans la requête. C'est la seule tolérance de `build_query`.
6. **`parse` accepte le libellé avec ou sans guillemets.** `label:"bug"` et `label:bug` donnent tous deux `Label("bug")`, qui se réécrit `label:"bug"`. Sans quoi la forme guillemetée — celle que `fragment` produit — ne se relirait pas elle-même.
7. **Un préfixe sans valeur n'est pas un filtre.** `org:`, `repo:`, `label:` seuls tombent en `Raw` et partent tels quels : inventer `org:` vide serait fabriquer une requête que l'utilisateur n'a pas écrite.
8. **`build_query(&[])` rend `is:pr sort:updated-desc`.** La fonction reste totale et sans panique ; le refus de la liste vide appartient à `config.rs`, qui l'applique déjà avant que `app` n'existe.

## Structure des fichiers

| Fichier | Responsabilité | Tâche |
|---|---|---|
| `src/filter.rs` | `Filter`, `Filter::fragment`, `Filter::parse`, `build_query`, et leurs tests purs | 1 puis 2 |
| `src/app.rs` | Traduit les chaînes des réglages en `Vec<Filter>`, assemble la requête, `Command::Fetch { query }` | 3 |
| `src/github/mod.rs` | `fetch_pull_requests(&self, query: &str, page_size: u16)`, `search_query` supprimée | 3 |
| `src/main.rs` | Passe `query` au client | 3 |
| `docs/specs/02-filtres.md` | Reçoit les décisions prises en cours de route | 3 |
| `docs/suivi/DETTE.md` | Perd l'entrée résolue | 3 |

Aucun fichier n'est créé. Les tests de `filter` sont des tests unitaires dans `src/filter.rs` : `owl` est un binaire, ses modules ne sont pas accessibles depuis un test d'intégration.

---

### Task 1: `Filter`, `fragment` et `build_query`

**Files:**
- Modify: `src/filter.rs` (fichier réduit à son commentaire de module, à remplacer entièrement)

**Interfaces:**
- Consumes: rien.
- Produces:
  - `pub enum Filter { AuthoredByMe, ReviewRequestedFromMe, AssignedToMe, Open, Draft(bool), Org(String), Repo(String), Label(String), Raw(String) }`, qui dérive `Debug, Clone, PartialEq, Eq`.
  - `pub fn Filter::fragment(&self) -> String`
  - `pub fn build_query(filters: &[Filter]) -> String`

- [ ] **Step 1: Créer la branche de travail depuis `develop`**

```bash
git switch develop
git pull --ff-only
git switch -c feat/filtres
```

- [ ] **Step 2: Écrire les tests qui échouent**

Remplacer tout le contenu de `src/filter.rs` par le commentaire de module attendu et ce bloc de tests. Le code de production vient à l'étape 4 : le fichier ne compile pas encore, c'est voulu.

```rust
//! Filtres et construction de la requête de recherche GitHub.
//!
//! Seul endroit du programme qui connaît la syntaxe de recherche de GitHub.
//! Ajouter un filtre coûte trois modifications, toutes ici : une variante,
//! une ligne dans `fragment`, une ligne dans `parse`.
//!
//! Le module est pur : ni réseau, ni terminal, ni réglages.

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn chaque_filtre_donne_le_fragment_de_la_spec() {
        let cas = [
            (Filter::AuthoredByMe, "author:@me"),
            (Filter::ReviewRequestedFromMe, "review-requested:@me"),
            (Filter::AssignedToMe, "assignee:@me"),
            (Filter::Open, "is:open"),
            (Filter::Draft(true), "draft:true"),
            (Filter::Draft(false), "draft:false"),
            (Filter::Org("acme".to_string()), "org:acme"),
            (Filter::Repo("acme/owl".to_string()), "repo:acme/owl"),
            (Filter::Label("bug".to_string()), "label:\"bug\""),
            (
                Filter::Raw("involves:@me -is:draft".to_string()),
                "involves:@me -is:draft",
            ),
        ];
        for (filtre, attendu) in cas {
            assert_eq!(filtre.fragment(), attendu, "filtre = {filtre:?}");
        }
    }

    #[test]
    fn un_libelle_a_espaces_reste_entre_guillemets() {
        assert_eq!(
            Filter::Label("needs review".to_string()).fragment(),
            "label:\"needs review\""
        );
    }

    #[test]
    fn les_filtres_par_defaut_donnent_la_chaine_de_la_spec() {
        assert_eq!(
            build_query(&[Filter::AuthoredByMe, Filter::Open]),
            "is:pr author:@me is:open sort:updated-desc"
        );
    }

    #[test]
    fn la_requete_porte_toujours_is_pr_en_tete_et_le_tri_en_queue() {
        let requete = build_query(&[Filter::Repo("acme/owl".to_string())]);
        assert_eq!(requete, "is:pr repo:acme/owl sort:updated-desc");

        // Y compris sans aucun filtre : la fonction reste totale. Le refus
        // d'une liste vide appartient à `config`, qui l'applique au démarrage.
        assert_eq!(build_query(&[]), "is:pr sort:updated-desc");
    }

    #[test]
    fn l_ordre_des_filtres_ne_change_pas_l_ensemble_ramene() {
        let un = build_query(&[Filter::AuthoredByMe, Filter::Open, Filter::Draft(false)]);
        let autre = build_query(&[Filter::Draft(false), Filter::Open, Filter::AuthoredByMe]);

        let cadre = |requete: &str| {
            let mots: Vec<String> = requete.split(' ').map(str::to_string).collect();
            (mots.first().cloned(), mots.last().cloned())
        };
        assert_eq!(cadre(&un), cadre(&autre), "is:pr en tête, tri en queue");

        let mut mots_un: Vec<&str> = un.split(' ').collect();
        let mut mots_autre: Vec<&str> = autre.split(' ').collect();
        mots_un.sort_unstable();
        mots_autre.sort_unstable();
        assert_eq!(
            mots_un, mots_autre,
            "les deux requêtes portent les mêmes termes, donc ramènent le même ensemble"
        );
    }

    #[test]
    fn un_fragment_vide_ne_laisse_pas_de_double_espace() {
        let requete = build_query(&[Filter::Raw(String::new()), Filter::Open]);
        assert_eq!(requete, "is:pr is:open sort:updated-desc");
        assert!(!requete.contains("  "), "requete = {requete}");
    }
}
```

- [ ] **Step 3: Lancer les tests pour vérifier qu'ils échouent**

Run: `cargo test filter 2>&1 | tail -20`
Expected: échec de compilation, `cannot find type Filter in this scope` et `cannot find function build_query in this scope`.

- [ ] **Step 4: Écrire l'implémentation minimale**

Insérer ce code dans `src/filter.rs`, entre le commentaire de module et le bloc `mod tests`.

```rust
/// Un filtre de recherche. Chaque variante sait produire son fragment de
/// chaîne ; c'est GitHub qui filtre, `owl` ne filtre jamais en mémoire.
#[derive(Debug, Clone, PartialEq, Eq)]
pub enum Filter {
    AuthoredByMe,
    ReviewRequestedFromMe,
    AssignedToMe,
    Open,
    Draft(bool),
    Org(String),
    Repo(String),
    Label(String),
    /// Soupape : n'importe quelle expression de recherche GitHub, transmise
    /// telle quelle, sans attendre qu'un filtre dédié existe.
    Raw(String),
}

/// Toujours en tête : `search` de type `ISSUE` ramène aussi des issues.
const PREFIXE: &str = "is:pr";

/// Toujours en queue : les pull requests récemment actives arrivent en premier.
const SUFFIXE: &str = "sort:updated-desc";

impl Filter {
    /// Fragment de requête de recherche GitHub.
    pub fn fragment(&self) -> String {
        match self {
            Filter::AuthoredByMe => "author:@me".to_string(),
            Filter::ReviewRequestedFromMe => "review-requested:@me".to_string(),
            Filter::AssignedToMe => "assignee:@me".to_string(),
            Filter::Open => "is:open".to_string(),
            Filter::Draft(true) => "draft:true".to_string(),
            Filter::Draft(false) => "draft:false".to_string(),
            Filter::Org(organisation) => format!("org:{organisation}"),
            Filter::Repo(depot) => format!("repo:{depot}"),
            // Les guillemets sont nécessaires : un libellé peut porter une
            // espace, qui séparerait sinon deux termes de recherche.
            Filter::Label(libelle) => format!("label:\"{libelle}\""),
            Filter::Raw(expression) => expression.clone(),
        }
    }
}

/// Assemble les fragments et garantit la présence de `is:pr` et du tri.
///
/// Les fragments vides sont écartés : une chaîne blanche dans les réglages
/// laisserait sinon une double espace au milieu de la requête.
pub fn build_query(filters: &[Filter]) -> String {
    let mut morceaux = Vec::with_capacity(filters.len() + 2);
    morceaux.push(PREFIXE.to_string());
    morceaux.extend(
        filters
            .iter()
            .map(Filter::fragment)
            .filter(|fragment| !fragment.is_empty()),
    );
    morceaux.push(SUFFIXE.to_string());
    morceaux.join(" ")
}
```

`Filter` n'a encore aucun appelant hors tests : ajouter `#![allow(dead_code)]` juste après le commentaire de module, avant toute déclaration, pour que `cargo clippy -- -D warnings` passe. La tâche 3 le retire.

- [ ] **Step 5: Lancer les tests pour vérifier qu'ils passent**

Run: `cargo test filter 2>&1 | tail -20`
Expected: six tests passent.

- [ ] **Step 6: Lancer les quatre commandes**

```bash
cargo build && cargo test && cargo clippy -- -D warnings && cargo fmt --check
```
Expected: tout passe. `cargo fmt --check` muet ; s'il signale un écart, lancer `cargo fmt` et relancer.

- [ ] **Step 7: Commit**

```bash
git add src/filter.rs docs/superpowers/plans/2026-09-02-filtres.md
git commit -m "Ajoute le type Filter et la construction de la requête de recherche"
```

---

### Task 2: Reconnaissance des chaînes de réglages

**Files:**
- Modify: `src/filter.rs`

**Interfaces:**
- Consumes: `Filter`, `Filter::fragment`, `build_query` de la tâche 1.
- Produces: `pub fn Filter::parse(texte: &str) -> Filter` — jamais d'erreur : une chaîne non reconnue devient `Filter::Raw`.

- [ ] **Step 1: Écrire les tests qui échouent**

Ajouter ces tests dans le `mod tests` de `src/filter.rs`.

```rust
    #[test]
    fn chaque_fragment_de_la_spec_se_relit_en_son_filtre() {
        let cas = [
            ("author:@me", Filter::AuthoredByMe),
            ("review-requested:@me", Filter::ReviewRequestedFromMe),
            ("assignee:@me", Filter::AssignedToMe),
            ("is:open", Filter::Open),
            ("draft:true", Filter::Draft(true)),
            ("draft:false", Filter::Draft(false)),
            ("org:acme", Filter::Org("acme".to_string())),
            ("repo:acme/owl", Filter::Repo("acme/owl".to_string())),
            ("label:\"bug\"", Filter::Label("bug".to_string())),
        ];
        for (texte, attendu) in cas {
            assert_eq!(Filter::parse(texte), attendu, "texte = {texte}");
        }
    }

    #[test]
    fn un_filtre_se_relit_depuis_son_propre_fragment() {
        let filtres = [
            Filter::AuthoredByMe,
            Filter::ReviewRequestedFromMe,
            Filter::AssignedToMe,
            Filter::Open,
            Filter::Draft(true),
            Filter::Draft(false),
            Filter::Org("acme".to_string()),
            Filter::Repo("acme/owl".to_string()),
            Filter::Label("needs review".to_string()),
        ];
        for filtre in filtres {
            assert_eq!(
                Filter::parse(&filtre.fragment()),
                filtre,
                "aller-retour cassé pour {filtre:?}"
            );
        }
    }

    #[test]
    fn un_libelle_sans_guillemets_est_accepte() {
        assert_eq!(
            Filter::parse("label:bug"),
            Filter::Label("bug".to_string())
        );
    }

    #[test]
    fn une_chaine_inconnue_devient_raw_et_reste_intacte() {
        let inconnue = "involves:@me -is:draft";
        assert_eq!(
            Filter::parse(inconnue),
            Filter::Raw(inconnue.to_string())
        );
        assert!(
            build_query(&[Filter::parse(inconnue)]).contains(inconnue),
            "l'expression doit traverser la requête sans retouche"
        );
    }

    #[test]
    fn un_prefixe_sans_valeur_reste_raw() {
        for texte in ["org:", "repo:", "label:", "draft:peut-etre", "is:closed"] {
            assert_eq!(
                Filter::parse(texte),
                Filter::Raw(texte.to_string()),
                "texte = {texte}"
            );
        }
    }

    #[test]
    fn les_espaces_autour_d_un_filtre_sont_ignorees() {
        assert_eq!(Filter::parse("  is:open  "), Filter::Open);
    }
```

- [ ] **Step 2: Lancer les tests pour vérifier qu'ils échouent**

Run: `cargo test filter 2>&1 | tail -20`
Expected: échec de compilation, `no function or associated item named parse found for enum Filter`.

- [ ] **Step 3: Écrire l'implémentation minimale**

Ajouter cette méthode dans le `impl Filter` de `src/filter.rs`, sous `fragment`.

```rust
    /// Reconnaît une chaîne du fichier de réglages. Une chaîne non reconnue
    /// devient `Raw`, ce qui la transmet à GitHub telle quelle : mieux vaut
    /// une expression que `owl` ne comprend pas qu'un filtre perdu.
    pub fn parse(texte: &str) -> Filter {
        let texte = texte.trim();

        match texte {
            "author:@me" => return Filter::AuthoredByMe,
            "review-requested:@me" => return Filter::ReviewRequestedFromMe,
            "assignee:@me" => return Filter::AssignedToMe,
            "is:open" => return Filter::Open,
            "draft:true" => return Filter::Draft(true),
            "draft:false" => return Filter::Draft(false),
            _ => {}
        }

        // Filtres à valeur. Un préfixe sans valeur n'est pas un filtre : il
        // part en `Raw` plutôt que de fabriquer un `org:` vide.
        for (prefixe, construire) in [
            ("org:", Filter::Org as fn(String) -> Filter),
            ("repo:", Filter::Repo as fn(String) -> Filter),
            ("label:", Filter::Label as fn(String) -> Filter),
        ] {
            if let Some(valeur) = texte.strip_prefix(prefixe) {
                // Les guillemets sont la forme que produit `fragment` ; les
                // retirer ici est ce qui rend l'aller-retour fidèle.
                let valeur = valeur.trim_matches('"');
                if !valeur.is_empty() {
                    return construire(valeur.to_string());
                }
            }
        }

        Filter::Raw(texte.to_string())
    }
```

- [ ] **Step 4: Lancer les tests pour vérifier qu'ils passent**

Run: `cargo test filter 2>&1 | tail -20`
Expected: douze tests passent.

- [ ] **Step 5: Lancer les quatre commandes**

```bash
cargo build && cargo test && cargo clippy -- -D warnings && cargo fmt --check
```
Expected: tout passe.

- [ ] **Step 6: Commit**

```bash
git add src/filter.rs
git commit -m "Reconnaît les filtres écrits dans le fichier de réglages"
```

---

### Task 3: Câblage — `app` assemble, `github` reçoit la chaîne

**Files:**
- Modify: `src/app.rs` (`Command::Fetch`, `App`, `App::new`, `App::fetch`, tests)
- Modify: `src/github/mod.rs:178-186` (`fetch_pull_requests`), `src/github/mod.rs:254-256` (`search_query`, supprimée), `src/github/mod.rs:300-310` et `:452-459` (tests)
- Modify: `src/main.rs:160-176` (`execute_command`)
- Modify: `src/filter.rs` (retrait de `#![allow(dead_code)]`)
- Modify: `docs/specs/02-filtres.md`
- Modify: `docs/suivi/DETTE.md`

**Interfaces:**
- Consumes: `filter::Filter`, `filter::Filter::parse`, `filter::build_query` des tâches 1 et 2.
- Produces:
  - `app::Command::Fetch { generation: Generation, query: String, page_size: u16 }` — le champ `filters: Vec<String>` disparaît.
  - `github::Client::fetch_pull_requests(&self, query: &str, page_size: u16) -> Result<ListPage, GithubError>`
  - `github::search_query` n'existe plus.

- [ ] **Step 1: Écrire le test qui échoue dans `app`**

Dans `src/app.rs`, remplacer le test `le_demarrage_emet_une_seule_requete` et le test `les_filtres_des_reglages_sont_transmis_a_la_requete` par ceux-ci, et ajouter les deux nouveaux.

```rust
    #[test]
    fn le_demarrage_emet_une_seule_requete() {
        let mut app = App::new(Config::default());
        let commandes = app.start();
        assert_eq!(
            commandes,
            vec![Command::Fetch {
                generation: 1,
                query: "is:pr author:@me is:open sort:updated-desc".to_string(),
                page_size: 50,
            }]
        );
        assert!(app.loading);
    }

    #[test]
    fn les_filtres_des_reglages_sont_transmis_a_la_requete() {
        let reglages = Config {
            filters: vec!["review-requested:@me".to_string()],
            page_size: 7,
            ..Config::default()
        };
        let mut app = App::new(reglages);
        assert_eq!(
            app.start(),
            vec![Command::Fetch {
                generation: 1,
                query: "is:pr review-requested:@me sort:updated-desc".to_string(),
                page_size: 7,
            }]
        );
    }

    #[test]
    fn un_filtre_inconnu_des_reglages_traverse_la_requete_intact() {
        let reglages = Config {
            filters: vec!["involves:@me -is:draft".to_string()],
            ..Config::default()
        };
        let mut app = App::new(reglages);
        match &app.start()[0] {
            Command::Fetch { query, .. } => assert_eq!(
                query,
                "is:pr involves:@me -is:draft sort:updated-desc"
            ),
            autre => panic!("commande inattendue : {autre:?}"),
        }
    }

    #[test]
    fn l_ordre_des_filtres_des_reglages_ne_change_pas_les_termes_ramenes() {
        let requete = |filtres: Vec<&str>| {
            let reglages = Config {
                filters: filtres.into_iter().map(str::to_string).collect(),
                ..Config::default()
            };
            match &App::new(reglages).start()[0] {
                Command::Fetch { query, .. } => query.clone(),
                autre => panic!("commande inattendue : {autre:?}"),
            }
        };

        let un = requete(vec!["author:@me", "is:open"]);
        let autre = requete(vec!["is:open", "author:@me"]);
        assert!(un.starts_with("is:pr "), "{un}");
        assert!(un.ends_with(" sort:updated-desc"), "{un}");

        let mut mots_un: Vec<&str> = un.split(' ').collect();
        let mut mots_autre: Vec<&str> = autre.split(' ').collect();
        mots_un.sort_unstable();
        mots_autre.sort_unstable();
        assert_eq!(mots_un, mots_autre);
    }
```

- [ ] **Step 2: Lancer les tests pour vérifier qu'ils échouent**

Run: `cargo test app 2>&1 | tail -20`
Expected: échec de compilation, `struct variant Command::Fetch has no field named query`.

- [ ] **Step 3: Modifier `app` pour assembler la requête**

Dans `src/app.rs`, ajouter l'import sous `use crate::config::Config;` :

```rust
use crate::filter::{self, Filter};
```

Remplacer la variante `Fetch` de `Command` :

```rust
#[derive(Debug, PartialEq)]
pub enum Command {
    Fetch {
        generation: Generation,
        /// Chaîne de recherche complète, assemblée par `filter::build_query`.
        query: String,
        page_size: u16,
    },
    Quit,
}
```

Dans `struct App`, ajouter un champ à côté de `config: Config`, qui reste en place — les specs suivantes y liront `refresh_interval` et `preferred_merge_method` :

```rust
    /// Filtres des réglages, traduits une seule fois. Un futur changement de
    /// filtre depuis l'écran n'aura que ce vecteur à modifier.
    filters: Vec<Filter>,
```

Dans `App::new`, renseigner ce champ avant de ranger les réglages. L'ordre compte : `config` est déplacé dans la structure, donc les filtres se traduisent d'abord.

```rust
            filters: config
                .filters
                .iter()
                .map(|texte| Filter::parse(texte))
                .collect(),
            config,
```

Et remplacer le corps de `fetch` :

```rust
    /// Ouvre une nouvelle génération et demande les données.
    fn fetch(&mut self) -> Command {
        self.generation += 1;
        self.loading = true;
        Command::Fetch {
            generation: self.generation,
            query: filter::build_query(&self.filters),
            page_size: self.config.page_size,
        }
    }
```

`App::new` garde sa signature `pub fn new(config: Config) -> Self` : `main` la construit toujours à partir des réglages.

- [ ] **Step 4: Lancer les tests de `app` pour vérifier qu'ils passent**

Run: `cargo test app 2>&1 | tail -20`
Expected: les tests de `app` passent. `cargo build` échoue encore : `main.rs` et les tests de `github` n'ont pas suivi. C'est l'étape suivante.

- [ ] **Step 5: Supprimer `search_query` et brancher `main`**

Dans `src/github/mod.rs`, remplacer la signature et le corps de `fetch_pull_requests` :

```rust
    pub async fn fetch_pull_requests(
        &self,
        query: &str,
        page_size: u16,
    ) -> Result<ListPage, GithubError> {
        let variables = json!({ "q": query, "n": page_size });
        let donnees: dto::ListData = self.execute(queries::LIST, variables).await?;
        Ok(donnees.to_list_page())
    }
```

Supprimer entièrement la fonction `search_query` et son commentaire, ainsi que le test `la_chaine_de_recherche_joint_les_filtres` : l'assemblage est passé dans `filter`, qui le teste. Dans le test `appel`, remplacer l'appel par :

```rust
        client
            .fetch_pull_requests("is:pr author:@me sort:updated-desc", 50)
            .await
```

Dans `src/main.rs`, dans `execute_command`, remplacer le motif et l'appel :

```rust
        Command::Fetch {
            generation,
            query,
            page_size,
        } => {
            let envoi = envoi.clone();
            let client = client.clone();
            tokio::spawn(async move {
                let resultat = client.fetch_pull_requests(&query, page_size).await;
                let _ = envoi.send(Event::Data {
                    generation,
                    result: resultat,
                });
            });
        }
```

Dans `src/filter.rs`, retirer `#![allow(dead_code)]` : `app` appelle maintenant `Filter::parse` et `build_query`. Si `cargo clippy -- -D warnings` signale malgré tout une variante jamais construite hors tests — `AssignedToMe`, `Draft`, `Org`, `Label` ne sont atteintes que par `parse`, donc toutes construites : aucun avertissement attendu.

- [ ] **Step 6: Lancer les quatre commandes**

```bash
cargo build && cargo test && cargo clippy -- -D warnings && cargo fmt --check
```
Expected: tout passe. Aucun test n'est perdu au change : le seul supprimé, `la_chaine_de_recherche_joint_les_filtres`, est remplacé par les tests de `build_query` de la tâche 1.

- [ ] **Step 7: Reporter les décisions dans la spec**

Dans `docs/specs/02-filtres.md`, remplacer entièrement la section « Note d'implémentation » par :

```markdown
## Note d'implémentation

`config::Config` expose `filters: Vec<String>` : les réglages portent les filtres
sous forme de chaînes, et c'est `app` qui les traduit en variantes de `Filter`, une
seule fois, à sa construction.

`app` assemble la requête avec `build_query` et la transmet dans
`Command::Fetch { generation, query, page_size }`. Le client GitHub reçoit donc une
chaîne toute faite et n'assemble plus rien : l'ancienne jointure `github::search_query`
a disparu, et la syntaxe de recherche ne vit plus qu'ici.

Trois précisions que le tableau des correspondances ne dit pas :

- `Filter::parse` accepte un libellé avec ou sans guillemets — `label:"bug"` comme
  `label:bug` donnent `Label("bug")`, qui se réécrit `label:"bug"`.
- Un préfixe sans valeur — `org:`, `repo:`, `label:` seuls — n'est pas reconnu et
  part en `Raw`, transmis tel quel.
- `build_query` écarte les fragments vides, et n'enlève aucun doublon : `is:pr`
  écrit dans les réglages apparaît deux fois dans la requête, ce que GitHub accepte.
```

- [ ] **Step 8: Retirer l'entrée résolue de la dette**

Dans `docs/suivi/DETTE.md`, supprimer entièrement la section « ### La chaîne de recherche est une jointure de filtres » et son contenu, jusqu'à la section suivante exclue. Le fichier n'est pas un historique : une entrée résolue en sort. Les deux autres entrées restent.

- [ ] **Step 9: Commit**

```bash
git add src/app.rs src/github/mod.rs src/main.rs src/filter.rs docs/specs/02-filtres.md docs/suivi/DETTE.md
git commit -m "Assemble la requête de recherche avec build_query"
```

---

### Task 4: Vérification et pull request

**Files:**
- Modify: aucun a priori. Un écart révélé ici se corrige dans le fichier concerné.

**Interfaces:**
- Consumes: tout ce que les tâches 1 à 3 produisent.
- Produces: rien. C'est une porte, pas un changement.

- [ ] **Step 1: Vérifier la requête réellement envoyée, contre le vrai GitHub**

```bash
python3 -c "
import re
s = open('src/github/queries.rs').read()
print(re.search(r'pub const LIST: &str = r#\"(.*?)\"#;', s, re.S).group(1))
" > /private/tmp/owl-list.graphql
gh api graphql -F q='is:pr author:@me is:open sort:updated-desc' -F n=5 -F query=@/private/tmp/owl-list.graphql | head -40
```

Expected: un objet JSON avec `data.search.nodes` ne contenant que des pull requests — donc plus aucun nœud vide venu d'une issue — et `data.rateLimit`. Vérifier aussi que les `updatedAt` sont décroissants : c'est ce que `sort:updated-desc` doit produire.

Si `gh` est absent ou non connecté, cette étape n'est pas réalisable : passer à la suivante sans poser de question et sans en faire mention dans le rapport final.

- [ ] **Step 2: Vérifier à l'œil dans le terminal**

Cette spec ne modifie aucun élément d'interface : ni écran, ni ligne, ni barre d'état. La vérification visuelle porte donc sur le seul effet observable, l'ordre de la liste. `claude-in-chrome` est inapplicable — `owl` est une interface en mode texte, pas une page web ; aucun compte de test à connecter.

```bash
cargo run
```

Expected: l'écran s'ouvre, la liste affiche des pull requests de la plus récemment active à la plus ancienne, la barre d'état annonce leur nombre, et `q` rend le terminal propre. Sans jeton disponible, l'étape n'est pas réalisable : le noter et passer à la suivante.

- [ ] **Step 3: Traiter un écart révélé par les étapes 1 ou 2**

Reproduire d'abord en test. Une expression de recherche réelle qui surprend s'ajoute aux tests de `src/filter.rs`, puis le test échoue, puis `fragment` ou `parse` est corrigée. Si l'écart porte sur une décision d'affichage — l'ordre visible, la troncature d'une ligne — il appartient à `03-affichage-et-navigation.md` : le consigner dans `docs/suivi/DETTE.md` plutôt que de le régler ici.

- [ ] **Step 4: Vérifier les critères de réussite de la spec, un par un**

```bash
cargo test 2>&1 | tail -30
```

Chaque critère de `02-filtres.md` doit pointer sur un test nommé :

| Critère de la spec | Test |
|---|---|
| Une liste de filtres donnée produit exactement la chaîne attendue, `is:pr` en tête et `sort:updated-desc` en queue | `les_filtres_par_defaut_donnent_la_chaine_de_la_spec`, `la_requete_porte_toujours_is_pr_en_tete_et_le_tri_en_queue`, `chaque_filtre_donne_le_fragment_de_la_spec` |
| L'ordre des filtres dans les réglages ne change pas l'ensemble des PR ramenées | `l_ordre_des_filtres_ne_change_pas_l_ensemble_ramene`, `l_ordre_des_filtres_des_reglages_ne_change_pas_les_termes_ramenes` |
| Une chaîne inconnue dans `filters` se retrouve intacte dans la requête | `une_chaine_inconnue_devient_raw_et_reste_intacte`, `un_filtre_inconnu_des_reglages_traverse_la_requete_intact` |
| Une liste `filters` vide est refusée avec un message explicite | `liste_de_filtres_vide_refusee_avec_son_propre_message` (déjà en place dans `src/config.rs`) |

- [ ] **Step 5: Vérifier que l'ajout d'un filtre ne coûte qu'un fichier**

C'est l'exigence centrale de la spec, et elle se vérifie par lecture, pas par test :

```bash
grep -rn "Filter\|build_query" src --include=*.rs | grep -v "^src/filter.rs"
```

Expected: uniquement `src/app.rs`, et seulement pour l'import, la traduction dans `App::new` et l'appel dans `fetch` — jamais un `match` sur les variantes. Un `match Filter` ailleurs que dans `filter.rs` casse l'exigence : le corriger avant d'ouvrir la pull request.

- [ ] **Step 6: Lancer les quatre commandes une dernière fois**

```bash
cargo build && cargo test && cargo clippy -- -D warnings && cargo fmt --check
```
Expected: tout passe.

- [ ] **Step 7: Commit s'il y a eu des corrections**

```bash
git add -A
git commit -m "Corrige les écarts relevés à la vérification sur le vrai GitHub"
```

S'il n'y a rien à corriger, aucun commit.

- [ ] **Step 8: Ouvrir la pull request**

Pousser la branche et ouvrir une pull request **vers `develop`**, jamais vers `main`. Titre et corps en français ; le corps renvoie à `docs/specs/02-filtres.md` et liste les critères de réussite couverts.

```bash
git push -u origin feat/filtres
gh pr create --base develop --title "Filtres et construction de la requête de recherche" --body "<corps en français>"
```

- [ ] **Step 9: Rapporter**

Le rapport final ne raconte pas le travail fait. Il donne uniquement, s'il y en a, ce que l'humain doit faire lui-même — un jeton à poser, un outil à installer — et le lien de la pull request. Rien sur `docs/suivi/DETTE.md`, rien sur les vérifications à l'œil, rien sur les manipulations git. S'il n'y a aucune action humaine à mener, le dire en une phrase avec le lien de la pull request.
